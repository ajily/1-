&НаСервереПроцедура ПриИзмененииТовара(ЭтоСобытие)    Если ЭтоСобытие.Колонка = ЭтоСобытие.Колонки.Товар Тогда        ЭлементыФормы = ЭтоСобытие.ЭлементыФормы;        СтрокаТовара = ЭлементыФормы.Товары.ТекущиеДанные;        Товар = СтрокаТовара.Товар;        Если Товар <> Неопределено Тогда            СтрокаТовара.Цена = Товар.ЦеныНоменклатуры.Получить(ТекущаяДата()).Цена;        КонецЕсли;    КонецЕсли;КонецПроцедуры